<Window x:Class="Essence.Elements.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
        xmlns:local="clr-namespace:Essence.Elements"
        mc:Ignorable="d"
        Title="Notificação"
        Width="300"
        Height="100"
        WindowStartupLocation="Manual"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        BorderBrush="Black"
        BorderThickness="1"
        WindowStyle="None">
    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="0" To="1" Duration="0:0:0.7" />
        </Storyboard>
        
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="1" To="0" Duration="0:0:0.45" />
        </Storyboard>
    </Window.Resources>
    
    <Border x:Name="border" Background="#7F000000" Padding="10" BorderThickness="1">
        <Border.BorderBrush>
            <LinearGradientBrush EndPoint="0.5,0" StartPoint="0,0.5">
                <GradientStop x:Name="Gradient" Color="#FFC12222" Offset="0.2"/>
                <GradientStop Color="Transparent" Offset="1.1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Grid>
            <Image x:Name="Image" Source="/ImageResources/app.ico" Width="60" Height="60" HorizontalAlignment="Left" Visibility="Hidden" RenderOptions.BitmapScalingMode="HighQuality" RenderOptions.EdgeMode="Aliased"/>

            <TextBlock x:Name="titulo" Text="Essence Notification" FontSize="16" FontWeight="Bold" TextWrapping="Wrap" Foreground="White" VerticalAlignment="Top" />
            <TextBlock x:Name="textox" Text="Essence is Injected." TextWrapping="Wrap" Foreground="White" FontSize="13.5" Margin="0,22,0,0" />
            
            <materialDesign:PackIcon x:Name="link_warning" Kind="LinkVariant" Foreground="#FFC8C8C8" HorizontalAlignment="Right" Margin="0,-5,-5,0" Visibility="Collapsed" />
            <materialDesign:PackIcon x:Name="information" Kind="InformationVariant" Foreground="#FFC8C8C8" HorizontalAlignment="Right" Margin="0,-5,-5,0" Visibility="Visible" />

        </Grid>
    </Border>
</Window>
